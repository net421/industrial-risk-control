# Next Actions

1. Optionally run `cloud-proof.yml` for ten minutes on a hosted runner.
2. Create an AWS/Oracle Ubuntu worker only when recurring long runs are needed.
3. Enroll a self-hosted runner only with repository and secret protections.
4. Perform live literature verification before any publishability claim.
5. Create a version tag only when a release artifact is intentionally required.
